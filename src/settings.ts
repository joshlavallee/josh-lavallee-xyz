export const CAMERA_SETTINGS = {
  fov: 45,
  near: 0.1,
  far: 200,
  position: [3, 2, 6] as [number, number, number],
}

export const PIXEL_RATIO: [number, number] = [1, 2]

export const GL_SETTINGS = {}
